enum SnackBarType { error, success, universal, warning }

enum RideStatus {
  started,
  not_started,
  finished,
}

enum ArrivalStatus{
  not_arrived,
  arrival_soon,
  arrived,
  picked,
  dropped,
  not_going,
}

enum RideShiftType { morning, evening, not_available }